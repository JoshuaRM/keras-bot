version: 2
jobs:
  build:
    docker:
      - image: python
    steps:
      - checkout
      - run:
          command: |
            cd enforce_codeowners
            docker build . -t dodo
            docker run dodo
